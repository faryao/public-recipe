# 术语表

## 食谱集

公开的食谱集合及其网站。

## 食谱条目

一道菜的记录，以 Markdown 编写，发布在独立的网页上。通过 slug（文件名）标识，不依赖日期。

## 阅读排版

显示时对食谱正文进行的间距调整，改善中英文混排的可读性，不修改源文件。

## 裸 URL

在食谱正文中直接写的网址（非 Markdown 链接格式），显示时自动转换为超链接。

## Slug

食谱文件的名字（不含 `.md` 后缀），用于生成网页的 URL 路径。例如 `mapo-tofu.md` 的 slug 是 `mapo-tofu`。
